const StudentCard=({student})=>{
    return(
        <div>
            <div className="card-header">
                <h3>{student.name}</h3>
                <span className=`${}`></span>
            </div>

        </div>
    )
}
export default StudentCard